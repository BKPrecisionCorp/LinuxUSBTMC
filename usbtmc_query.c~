#include <sys/ioctl.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[]){
  
  int instr,i;
  char* ret;
  ret = malloc(32);
  printf("%d arguments\n", argc);
  for(i=1; i<argc; i++){
    printf("%s \n", argv[i]);
  }
  //instr = open("/dev/usbtmc0", O_RDWR);
  //write(instr, "system:remote\n",14);
  //  read(instr, ret, 32);
  //  printf("%s\n",ret);
  return 0;

}
